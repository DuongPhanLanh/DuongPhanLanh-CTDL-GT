#include<iostream>
using namespace std;


float deQuy(int n) // đệ quy
{
    if(n == 1)
        return 1;
    return deQuy(n - 1) * n;
}
int main()
{
    int n;
    printf("\nNhap n: "); scanf("%d", &n);

    
    printf("\nDe quy(%d) = %f", n, deQuy(n));
    return 0;
}
